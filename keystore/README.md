# Upload keystore

Play App Signing: Google holds the app signing key. This folder holds the **upload** key we use to sign AABs.

`upload.jks` and `keystore.properties` are gitignored. Back them up off this machine. Lose the upload key and you request a reset in Play Console. Annoying, not fatal.

Do not commit those two files.
